<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tweetPockets="clr-namespace:TweetPockets;assembly=TweetPockets"
             x:Class="TweetPockets.TimelinePage"
             Title="TIMELINE">
  <Grid>
    <tweetPockets:TimelineListView ItemsSource="{Binding Timeline}"
                                   IsPullToRefreshEnabled="True"
                                   IsRefreshing="{Binding IsLoading}"
                                   RefreshCommand="{Binding LoadNewCommand}"
                                   LoadOldCommand="{Binding LoadOldCommand}"
                                   DismissCommand="{Binding MoveToReadLaterCommand}"/>
    
    <tweetPockets:TweetButton Margin="0,0,13,13" HorizontalOptions="End" VerticalOptions="End"
                              />
  </Grid>
</ContentPage>